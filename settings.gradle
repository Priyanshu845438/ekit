pluginManagement { // For plugin repositories
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // This line enforces the rule
    repositories {
        google() // Add Google repository here for library dependencies
        mavenCentral() // Add Maven Central here for library dependencies
        // Add any other repositories your project needs for libraries (e.g., jcenter(), custom Maven repos)
    }
}
rootProject.name = "E-ToolKit"
include ':app' // Or whatever your modules are