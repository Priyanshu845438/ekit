buildscript {
    repositories {
        google()
        mavenCentral()
        // jcenter() ← Remove this line
    }
    dependencies {
        // your classpath dependencies here
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        // jcenter() ← Remove this line
    }
}
